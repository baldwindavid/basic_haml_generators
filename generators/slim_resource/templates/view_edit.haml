%h2 Edit <%= singular_name %>

= error_messages_for :<%= singular_name %>

- semantic_form_for resource, :url => resource_path(resource), :html => {:method => :put} do |f|
  = render :partial => "<%= singular_name %>_form", :locals => {:f => f} 
  = f.buttons
  
= link_to "Show", resource_path(@<%= singular_name %>) 
\|
= link_to "Back", collection_path